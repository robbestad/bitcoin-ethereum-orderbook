export enum Feed {
  Bitcoin_USD = "PI_XBTUSD",
  Ethereum_USD = "PI_ETHUSD",
}

export enum TicketSize {
  BTCDefault = 0.5,
  BTCMedium = 1,
  BTCLarge = 2.5,
  EthDefault = 0.05,
  EthMedium = 0.1,
  EthLarge = 0.25,
}

export enum Variant {
  desktop,
  mobile,
}

export enum ReducerName {
  reset = "reset",
}
